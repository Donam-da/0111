<UserControl x:Class="namm.SelectCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="THANH TOÁN &amp; CHỌN KHÁCH HÀNG" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="tbBillInfo" HorizontalAlignment="Center" FontSize="16" Margin="0,5,0,0" Foreground="DarkSlateGray"/>
            <Separator Margin="0,10,0,10"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Cột trái: Danh sách khách hàng -->
            <DockPanel Grid.Column="0" Margin="0,0,10,0">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Tìm khách hàng (theo Tên hoặc SĐT):" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtSearchCustomer" TextChanged="TxtSearchCustomer_TextChanged"/>
                </StackPanel>
                <TextBlock x:Name="tbSelectedCustomer" DockPanel.Dock="Bottom" Text="(Chưa chọn khách hàng)" FontStyle="Italic" Margin="0,5,0,0"/>
                <DataGrid x:Name="dgCustomers" Margin="0,10,0,0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          LoadingRow="DgCustomers_LoadingRow" SelectionChanged="DgCustomers_SelectionChanged" SelectedValuePath="ID">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="40"/>
                        <DataGridTextColumn Header="Tên khách hàng" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Số điện thoại" Binding="{Binding PhoneNumber}" Width="120"/>
                        <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <!-- Cột phải: Thêm khách hàng mới và thanh toán -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <GroupBox Header="Thêm khách hàng mới" FontWeight="Bold">
                    <StackPanel Margin="5">
                        <TextBlock Text="Tên khách hàng (*):"/>
                        <TextBox x:Name="txtNewCustomerName" Margin="0,2,0,5"/>
                        <TextBlock Text="Số điện thoại:"/>
                        <TextBox x:Name="txtNewCustomerPhone" Margin="0,2,0,5"/>
                        <TextBlock Text="Địa chỉ:"/>
                        <TextBox x:Name="txtNewCustomerAddress" Margin="0,2,0,5"/>
                        <Button x:Name="btnAddNewCustomer" Content="Thêm &amp; Chọn" Click="BtnAddNewCustomer_Click"/>
                    </StackPanel>
                </GroupBox>

                <Separator Margin="0,20,0,20"/>

                <Button x:Name="btnPay" Content="Thanh Toán" Height="40" FontSize="16" FontWeight="Bold" Background="#4CAF50" Foreground="White" IsEnabled="False" Click="BtnPay_Click"/>
                <Button x:Name="btnPayAsGuest" Content="Thanh toán (Khách vãng lai)" Margin="0,10,0,0" Click="BtnPay_Click"/>
                <Button x:Name="btnBack" Content="Quay lại" Margin="0,10,0,0" Click="BtnBack_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>